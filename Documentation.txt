	Ћюба€ команда занимает место в пам€ти!
	 оманды раздел€ютс€ знаком ;

	#i - обращение к €чейке с номером 
	input (address, "Comments") - считывает данные из консоли и помещает их в €чейку с адресом address 
		(можно указать текст, который будет выведен в консоль при запросе значени€
	output (address) - выводит данные из €чейки address
	goto (address) - безусловный переход к €чейке address
	jump (address, b) - переход к €чейке с адресом address, если b - true
	place (a, address) - запись a в €чейку с адресом address
	sum (a, b, address) - помещает в €чейку с адресом address результат сложени€ двух чисел a и b
	substract (a, b, address) - помещает в €чейку с адресом address разность a и b
	divide (a, b, address) - помещает в €чейку с адресом address целую часть числа a / b
	multiply (a, b, address) - помещает в €чейку с адресом address произведение чисел a и b
	loworeq (a, b, address) - помещает в €чейку с адресом address true, если а <= b, иначе помещает false